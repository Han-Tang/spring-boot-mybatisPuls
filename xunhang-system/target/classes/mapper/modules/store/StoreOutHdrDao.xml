<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xunhang.modules.store.dao.StoreOutHdrDao">


    <!--<select id="queryListByCondition" resultType="cn.xunhang.modules.store.entity.StoreOutHdr">-->

        <!--select hdr.* from StoreOutHdr hdr where hdr.id in-->
        <!--(-->
            <!--select distinct(hdr.id) from StoreOutHdr hdr inner  join StoreOutDlt dlt on hdr.id = dlt.storeOutId-->
            <!--where 1 = 1-->
            <!--<if test="status!= null and status!= ''"> and dlt.status = #{status}</if>-->
            <!--<if test="code!= null and code!= ''"> and dlt.code = #{code}</if>-->
            <!--<if test="code!= null and code!= ''"> and dlt.code = #{code}</if>-->

        <!--)-->

    <!--</select>-->
</mapper>
